name: Release dry run

on:
  workflow_dispatch:
  pull_request:
    paths:
      - releasebox.config.json
      - package.json
      - package-lock.json
      - .github/workflows/release*.yml

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  dry-run:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Install package dependencies
        run: npm ci

      - name: Install ReleaseBox
        run: |
          git clone --branch v0.1.0 --depth 1 https://github.com/rogerchappel/releasebox.git /tmp/releasebox
          npm ci --prefix /tmp/releasebox
          npm run build --prefix /tmp/releasebox

      - name: Run ReleaseBox readiness check
        run: node /tmp/releasebox/bin/releasebox.js check .

      - name: Run release check
        run: npm run release:check

      - name: Generate ReleaseBox notes preview
        run: node /tmp/releasebox/bin/releasebox.js notes . > release-notes-preview.md

      - name: Upload ReleaseBox notes preview
        uses: actions/upload-artifact@v4
        with:
          name: release-notes-preview
          path: release-notes-preview.md
